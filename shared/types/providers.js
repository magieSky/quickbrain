// Re-export the production provider list. The single source of truth is main/ai/providers.js.
import { PROVIDERS, getProvider } from '../../client/src/main/ai/providers.js'
export { PROVIDERS, getProvider }
export default { PROVIDERS, getProvider }